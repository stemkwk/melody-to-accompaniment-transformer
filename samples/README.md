Samples generated with the final model (best-epoch=007) at the tuned inference defaults (temperature 1.2 / top_p 0.97).

Per song:
  01_melody.wav        - input melody (conditioning)
  02_ground_truth.wav  - melody + original POP909 accompaniment
  03_generated_ep7.wav - melody + model-generated accompaniment

Note: POP909 melodies enter around bar 4-8, so the first ~10s is silent (expected).
